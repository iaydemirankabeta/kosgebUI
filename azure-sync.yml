#!/bin/bash

if [ -d "kosgebUI" ]; then
  rm -rf kosgebUI
fi

git clone -b Production https://berksah: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@github.com/emrah136/kosgebUI
cd kosgebUI
git checkout Production 
rm -rf .git
cd ..

if [ -d "KOSGEB.SupportDigital_KOSGEBUI" ]; then
  rm -rf KOSGEB.SupportDigital_KOSGEBUI
fi

git clone https://28684755980:Eb.160823@tfs.kosgeb.gov.tr/tfs/DefaultCollection/KOSGEB.SupplierDigital/_git/KOSGEB.SupportDigital_KOSGEBUI
cd KOSGEB.SupportDigital_KOSGEBUI
git checkout Production 
cd ..

xcopy /E /Y kosgebUI/* KOSGEB.SupportDigital_KOSGEBUI/
cd KOSGEB.SupportDigital_KOSGEBUI
git config --global user.name "28684755980"
git add .
git commit -m "sync from git to azure"
git push
